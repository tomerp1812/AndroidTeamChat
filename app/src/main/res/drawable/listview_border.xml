<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" />
    <stroke android:color="@color/material_dynamic_primary40"
    android:width="2dp" />
    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
         /> <!-- Padding to adjust the content inside the border -->
</shape>
